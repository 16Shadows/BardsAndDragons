name: deploy
on:
  push:
    branches:
      - master
      - developer
      - feat-pipeline
  workflow_dispatch:

  
jobs:
  build:
    runs-on: local-debian
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Environment
        run: |
          if [ ${{ github.ref }} == 'refs/heads/main' ]; then echo "BUILD_TARGET=prod" >> GITHUB_ENV; fi
        shell: bash
      - name: Build docker
        env:
          BUILD_TARGET: production
        run: |
          docker build --cache-from $IMAGE -t bad:v1 .

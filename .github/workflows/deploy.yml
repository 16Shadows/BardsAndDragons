name: deploy
on:
  push:
    branches:
      - master
      - feat-pipeline
  workflow_dispatch:

  
jobs:
  build:
    runs-on: [self-hosted]
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Environment
        run: |
          echo "${{ secrets.DATABASE_HOSTNAME }}" > .env
          echo "${{ secrets.DATABASE_PASSWORD }}" >> .env
          echo "${{ secrets.DATABASE_PORT }}" >> .env
          echo "${{ secrets.DATABASE_USERNAME }}" >> .env
          echo "${{ secrets.JWT_SECRET_KEY }}" >> .env
        shell: bash
      - name: Build docker
        env:
          IMAGE: bad:v1
          STORAGE_PATH: /prog/web_dad/storage/userimages
          CONTAINER_NAME: bad-app
          NETWORK_NAME: bad-network
        run: |
          docker stop $CONTAINER_NAME && docker rm $CONTAINER_NAME || true
          docker build --cache-from $IMAGE -t $IMAGE .
          docker run -d -p 3000:3000 --name $CONTAINER_NAME --network $NETWORK_NAME -v $STORAGE_PATH:/app/public/userimages $IMAGE
